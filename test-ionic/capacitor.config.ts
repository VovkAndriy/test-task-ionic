import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.khbcode.learning',
  appName: 'test_tabs',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
